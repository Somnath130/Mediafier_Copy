Follow the steps below to run the app-:

1. Firstly open up the new terminal and Type (npm install nodemon -D). This will install nodemon on your system.

2. Then on the terminal run the command (npm run dev). This will connect to the local host

3. Now go to your browser and type (Localhost:3000)
 
4. This will run your chat application.